- name: conditions
  hosts: local
  connection: local
  vars:
    number: 5
  tasks:
  - name: check the number is less than 10
    ansible.builtin.debug:
      msg: given number {{number}} is less than 10
    when: number < 10