- name: argument level Variables
  hosts: local
  connection: local 
  tasks:
  - name: print the cousre info
    ansible.builtin.debug:  
      msg: "Course Name is: {{ COURSE }}, DUration: {{ DURATION }}, Trainer: {{ TRAINER}}"

#ansible-playbook -i inventory.ini -e ansible_user=ec2-user -e ansible_password=DevOps321 09-vars-args.yaml -e "COURSE=Ansible" -e "DURATION=10" -e "TRAINER=Reddy"
